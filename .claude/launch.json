{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "backend",
      "runtimeExecutable": "sh",
      "runtimeArgs": ["-c", "backend/.venv/bin/uvicorn main:app --reload --app-dir backend --port ${PORT:-8000}"],
      "autoPort": true,
      "port": 8000
    }
  ]
}

# Root-level requirements so Railway's Python builder detects the app.
# Deploys install the pinned lock; local dev uses backend/requirements.txt
# (the friendly lower-bound file) per the README.
-r backend/requirements.lock
